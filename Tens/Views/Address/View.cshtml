@model Tens.Models.addresse_type
@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container-fluid">
	<div class="col-lg-12 col-md-12 col-sm-12 col-sx-12">
		<div class="panel-body">
			<div class="row">
				<div class="pull-right">
					<a href="@Url.Content("~/Address")" data-toggle="tooltip" title="Back To Grid" class="btn btn-default"><i class="fa fa-refresh"></i> Back</a>
				</div>
			</div>
		</div>
	</div>
	<div class="col-lg-12 col-md-12 col-sm-12 col-sx-12">
		<div class="panel panel-default">
			<div class="panel-heading">
				<h3 class="panel-title">Address</h3>
			</div>
			<div class="panel-body">
				@using (Html.BeginForm("Create", "Address", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal" }))
                { 
                     <div class="form-group required">
						<label class="col-sm-2 control-label">Address Code</label>
						<div class="col-sm-6">
							 @Html.LabelFor(model => model.address_type_code)
						</div>
					</div>
                    <div class="form-group required">
						<label class="col-sm-2 control-label">Address Name</label>
						<div class="col-sm-6">
							 @Html.LabelFor(model => model.address_type_description)
						</div>
					</div>
					
                }
			</div>
		</div>
	</div>
</div>

